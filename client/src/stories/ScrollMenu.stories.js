import React from "react";
import { ScrollMenu } from "../components/ScrollMenu";

export default {
  title: "Component/ScrollMenu",
  component: ScrollMenu,
};

export const Standard = () => <ScrollMenu />;
